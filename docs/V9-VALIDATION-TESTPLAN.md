# V9 validation test plan

- existing regression suite must stay green
- TNL 118 detail confirmation must render as one question + one field
- TNL 002 overlap must render as one single-choice decision
- overlap must not use error-red semantics
- assisted and overlap flows must expose next-item continuation when another pending item exists
- production TypeScript build must succeed
